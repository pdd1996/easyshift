# 本地开发：API 默认运行在 http://localhost:3000
VITE_API_BASE_URL=http://localhost:3000/api/v1

# 生产推荐同域反代时使用：
# VITE_API_BASE_URL=/api/v1

# 若 Web / API 分域部署，改为完整 API 地址：
# VITE_API_BASE_URL=https://api.example.com/api/v1

# 线上建议在部署平台环境变量中配置，或使用不提交的 .env.production.local。
# VITE_* 变量会在构建时注入，修改后需要重新 build / redeploy。
